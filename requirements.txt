# requirements.txt (Tam ve Doğru Hali)

# Flask web sunucusu için
Flask
gunicorn
psycopg2-binary
requests

# Selenium botunun çalışması için GEREKLİ OLANLAR (Browserless ile kullanım)
# Sadece requests ile API çağrısı yapıldığı için selenium gerekmez.
# Ancak projede başka yerlerde kullanılmadığından emin olmak için temizliyoruz.

